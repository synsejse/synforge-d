# API URL the production bundle calls (leave empty for same-origin).
PUBLIC_API_URL=

# Where `npm run dev` should proxy /api and /repo to. Falls back to
# PUBLIC_API_URL, then http://localhost:8080.
VITE_DEV_PROXY_TARGET=
